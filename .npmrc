# dsh-context-show 依赖宿主提供官方 @deepseek-ai peer 包（0.1.3-alpha.1 在 npm
# 发布前无法独立安装）。关闭 peer 自动安装：宿主 profile 满足 peer，本地只装
# registry 可解析的 dependencies/devDependencies。
auto-install-peers=false
strict-peer-dependencies=false
